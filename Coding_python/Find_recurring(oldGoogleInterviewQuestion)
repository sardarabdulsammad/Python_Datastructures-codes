def recurring(arr):
  hashmap={}
  for i in range(0,(len(arr)-1)):
    if arr[i] in hashmap.keys():
      return arr[i]     
    else:
      hashmap[arr[i]]=i 
      

arr=[1,2,2,3,3,4,5,6]
recurring(arr)
